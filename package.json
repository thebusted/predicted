{
  "name": "predicted",
  "version": "1.0.0",
  "description": "Count person from video files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "http-server",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,./package-lock.json --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [
    "nodejs",
    "javascript"
  ],
  "author": "SUMETH TONGKAM",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "braces": ">=2.3.1",
    "minimist": ">=1.2.3",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "express": "^4.16.3"
  }
}
